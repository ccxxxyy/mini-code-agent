# Changelog

## Unreleased

### Added 新增

- **/undo selective restore + configurable snapshot capacity** — `/undo [N] [--code-only | --conv-only]`: default dual rollback unchanged; `--code-only` restores files only (conversation and turn counters untouched — keep the discussion, drop the changes; reports "nothing to restore" explicitly when no files were touched); `--conv-only` rolls back the conversation only (files kept as-is; the undone turns' snapshots are discarded via new `discard_turns()` so reused turn ids can't wrongly restore future turns to stale states). Snapshot retention is now configurable: `[memory] undo_keep_turns = N` (default still 5) flows into `FileSnapshotStore(keep_turns=...)`. When `/undo N` covers turns beyond the retention window, a warning line reports that those turns' file changes were NOT restored (silent skipping surfaced by real-terminal testing). 10 new tests, suite 1327→1337; real-LLM three-mode + retention-warning verification (file deleted + conversation kept / file kept + conversation rolled back / dual-rollback regression / pruned-turn warning). /undo 选择性恢复 + 快照容量配置：`/undo [N] [--code-only | --conv-only]`——默认双回滚不变；`--code-only` 仅恢复文件（对话与轮次计数不动，留讨论扔改动；无文件改动时明确报告）；`--conv-only` 仅回滚对话（文件保持现状，被撤销轮次快照经新增 `discard_turns()` 丢弃，防轮次编号复用后错误恢复）。快照保留轮数配置化：`[memory] undo_keep_turns = N`（默认仍 5）。`/undo N` 覆盖超出保留窗口的轮次时明确警告该部分文件改动未恢复（终端实测暴露的静默边界）。10 个新测试，1327→1337；真实 LLM 三模式 + 超窗警告验证。

- **MCP native lazy loading mode (Anthropic `defer_loading`)** — third MCP loading mode `loading = "native"`: MCP tools are registered in the ToolRegistry with `defer_loading: true`, the Anthropic server hides their schemas until the model calls `tool_search` which returns `tool_reference` blocks to expand them — no `mcp_call` relay needed, and the tools[] array stays stable to protect prompt cache prefixes. `anthropic-beta: advanced-tool-use-2025-11-20` header auto-attached. Non-Anthropic endpoints auto-fallback to dispatch. `_adjust_mcp_meta_tools()` dynamically unregisters unneeded meta tools by effective mode. 26 new tests, suite 1301→1327. MCP native 延迟加载模式（Anthropic `defer_loading`）：第三种 MCP 加载模式 `loading = "native"`——MCP 工具注册到 ToolRegistry 但带 `defer_loading: true`，Anthropic 服务端隐藏 schema 直到模型调 `tool_search` 返回 `tool_reference` 块展开——无需 `mcp_call` 中转，且 tools[] 数组不变保护 prompt cache 前缀。`anthropic-beta` header 自动附加。非 Anthropic 端点自动降级 dispatch。`_adjust_mcp_meta_tools()` 按生效模式动态注销不需要的 meta 工具。26 个新测试，1301→1327。

- **Request-side extended thinking control** — mini previously only PARSED thinking streams (reasoning_content / thinking_delta rendering) and never enabled them request-side, so Anthropic extended-thinking models never thought at all (DeepSeek-style models that emit reasoning_content automatically were unaffected). New `[llm] thinking = true` (or `MINI_AGENT_THINKING=true`, or per-profile `MODEL_<NAME>_THINKING=true`): the Anthropic provider sends `thinking: {type: enabled, budget_tokens}` with an adaptive budget (opus/sonnet >= 4.6 pass 0 so the model decides; others get max(1024, max_tokens-1), bumping max_tokens when the floor is hit), parses `signature_delta`, and round-trips SIGNED thinking blocks before tool_use in assistant messages (three gates: only when enabled / never without a signature / thinking block first). The OpenAI Responses provider sends `reasoning: {effort, summary: auto}` (effort via `extra.reasoning_effort`, default medium). Fixed en route: `to_api_messages()` never emitted metadata, so the existing Responses reasoning round-trip was dead code — now both providers share the metadata channel and the Chat Completions provider strips it before sending; all three providers' SSE parsers accepted only `data: ` with a space — per the SSE spec `data:` alone is valid and some Anthropic-compatible gateways send exactly that (every event was silently dropped); the Anthropic provider now sends both `x-api-key` and `Authorization: Bearer` (third-party gateways only read the latter). Verified: 16 new tests (7 assert the actual HTTP body via MockTransport), suite 1259->1275; real thinking stream + multi-turn tool use over an Anthropic-protocol endpoint (deepseek-v4-pro); local mock-SSE e2e (`experiments/verify_thinking_e2e.py`, 4/4) proving the signed-block round-trip through the full agent_loop pipeline. Remaining honest boundary: only the official Anthropic API enforces cryptographic signature validation. 发送侧 extended thinking 控制：mini 原来只被动解析思考流、从不在请求侧开启——Anthropic extended thinking 模型等于不会思考。新增 `[llm] thinking = true`（或 MINI_AGENT_THINKING / 按 profile 的 MODEL_<名>_THINKING）：Anthropic 请求体加自适应 budget 的 thinking 参数（opus/sonnet≥4.6 传 0 由模型自主决定，其余 max(1024, max_tokens−1) 且触下限时抬 max_tokens），解析 signature_delta，带签名 thinking 块在 tool_use 前回传（三道闸：仅开启时/无签名不回传/思考块在前）；Responses 加 reasoning 参数（effort 经 extra.reasoning_effort 配置）。顺带修复：to_api_messages 不输出 metadata 的死代码（原 Responses reasoning 回传拿不到数据）、三 Provider SSE 只认 `data: ` 带空格（兼容网关的无空格事件被全量静默丢弃）、Anthropic 补 Bearer 认证头。16 个新测试（7 个 MockTransport 请求体取证），1259→1275；Anthropic 协议端点真实思考流 + 工具多轮验证；本地 mock e2e 4/4 验签名往返全链路。诚实边界：签名密码学校验只有官方 API 执行。

- **OpenAI-compatible `extra` params actually passed through** — config-guide documented `[llm] extra = {}` as API pass-through but the Chat Completions provider never read it; hybrid reasoning models that gate thinking behind a request param (qwen3's `enable_thinking`) could not be switched. Now merged into the request body via setdefault (core fields like model/messages cannot be clobbered). Real-run evidence (qwen3.6-plus): `extra = {enable_thinking = false}` drops the thinking stream to zero — and the model then answers the 9.11-vs-9.9 trap WRONG, a live demo of thinking's quality value. 1 new test, 1275→1276. OpenAI 兼容 extra 参数真实透传：文档写了透传但代码没接，qwen3 类混合推理模型的 enable_thinking 开关此前无法生效；现按键 setdefault 合入请求体（核心字段不可覆盖）。真实验证：关思考后 9.11 vs 9.9 答错、开思考答对。1 个新测试，1275→1276。

- **Bash side-effect double-execution on truncation retry fixed** — when a max_tokens truncation triggers a retry, eagerly executed tool tasks that already completed are now cached by `(name, args_json)` signature (`_eager_completed`); if the retry produces the same tool call, the cached result is reused via a pre-resolved Future instead of re-executing — no command-intent classification needed, no streaming latency sacrifice. WRITE-category tools remain deferred by the category gate and never enter the cache path. 3 new tests, suite 1155→1158. bash 副作用截断重试双执行修复：截断重试时已 eager 完成的工具任务结果按签名缓存，重试产出相同签名时复用结果不重跑——无需命令意图分类，不牺牲流式延迟。WRITE 类工具仍走延迟路径，不进缓存。3 个新测试，1155→1158。

- **Recovery attachment includes skill invocation records** — after compression, the recovery attachment now lists active skills (marked "do NOT re-activate" -- their prompts survive in the never-compressed system prompt; what the summary swallowed was the ACTIVATION history) and previously-used-now-deactivated skills. The compact boundary persists `skill_invocations`/`active_skills`; on session restore `SkillRegistry.restore_state()` rebuilds the active set WITHOUT re-injecting prompts (the restored system_prompt already has them) — `is_active`/`deactivate`/`match_triggers`/`reload` all work again after a restore. Wired via a provider callback so the memory layer never imports the extensions layer. Re-verification exposed a pre-existing gap: manual `/compact` called the compressor directly, skipping the recovery attachment and EVERY boundary field (read files / last user request / skill state) — it now goes through `check_and_compress(force=True)`, the same pipeline as auto-compression. 12 new tests, suite 1143→1155. 压缩恢复附件含 skill 调用记录：激活中技能标注勿重复激活（prompt 在 system prompt 存活，丢的是激活历史）、已停用单列；边界持久化技能字段；会话恢复经 restore_state() 重建激活集合且不重注入 prompt；回调注入保持层级依赖方向；复验暴露既有缺陷——手动 /compact 直调 compressor 跳过恢复附件与全部边界字段，现改走 check_and_compress(force=True) 与自动压缩同一管道。12 个新测试，1143→1155。

- **Instruction file @-include** — `@./path` and `@~/path` lines in instruction files (AGENT.md / CLAUDE.md / .mini-agent/instructions.md / user-level) are recursively expanded to the referenced file's content, resolved relative to the including file's directory. Max depth 5 (configurable via `[context] max_include_depth`, 0 to disable); circular includes and missing files degrade gracefully to comment markers. 10 new tests, suite 1133→1143. 指令文件 @-include：`@./path` 和 `@~/path` 行递归展开为引用文件内容（相对于引用方目录解析），深度 5（可配置，0 禁用），循环/缺失注释降级。10 个新测试，1133→1143。
- **Tool category axis + sub-agent permission stack propagation** — the permission matrix gains its second axis: every Tool declares a `ToolCategory` (read / write / execute / external; undeclared plugin tools default to EXTERNAL, a snapshot test forces a conscious classification for new tools). New matrix cells: plan×WRITE denied BEFORE path checks (catches `install_skill`, which has no path argument), plan×EXTERNAL denied (MCP side effects cannot be verified read-only), bypass×EXTERNAL granted explicitly. Five drifted tool-name lists unified into categories (agent_loop's `_WRITE_TOOLS`, team.py's local copy that had lost delete_file, the permission routing tuples, `would_ask`'s list, and the plan schema filter). In-process sub-agents finally get a permission gate: `SubAgentManager.spawn()` passes each child a `ChildPermissionManager` view — full parent gate (deny rules / sensitive paths / dangerous commands / mode matrix) with shared-by-reference rules and a live `mode` property (`/deny` and `/mode` in the main session affect running sub-agents instantly), but NO confirm callback: anything that would prompt is denied fail-safe, which also dissolves the concurrent-dialog problem. The category gate reads `pm.mode` (not the loop's plan flag) — that is what makes propagation bite for children. Consequently plan mode's spawn ban became conditional: with a propagated gate, research agents may spawn (their writes are denied at the permission layer); without one, spawning stays disabled. Real-run also caught `ask_user`'s dialog being buried by live streaming (it eager-executed mid-stream); Tool ABC gained an `opens_dialog` attribute (ask_user / exit_plan_mode) and the streaming defer checks it instead of a name special-case. Propagation real-runs then exposed two behavior gaps, both fixed: gated sub-agents kept hunting bypass routes after denials (9 tools over 10 iterations) — `no_ui:default_deny` now counts toward the confirm-denial breaker (policy denials stay neutral); and the main agent burned 347K tokens tracing a session /deny rule that lives only in memory — denial reasons now carry the rule's source (`rule:ping* [/deny session rule, not persisted]`). Re-verification then caught two more: the bracket-formatted rule source crashed the Rich trace renderer (MarkupError — switched to parentheses and escaped ALL dynamic trace fields), and a sub-agent REALLY bypassed a deny rule by writing `run_ping.bat` and invoking it bare (the write-then-execute regexes missed the bare form; `is_executing_written_script` now checks each command segment's first token against the written-file set, plus call/start launchers — reading a written file stays untriggered). A breaker-stopped child's report now lists ALL distinct denial reasons in order (root cause first, via `AgentState.denial_reasons`) plus files it created — carrying only the breaker-tripping denial made the parent misdiagnose a deny rule as a missing confirm UI; carrying nothing made it blindly re-spawn an identical child, leaving orphan .bat files. Rule denials also carry a VERBATIM removal command (`/deny remove command "ping*"`) and the fact that deny rules bind every agent in the session -- a placeholder form got garbled into a nonexistent command, and without the global-binding fact the parent offered to run the denied command itself. Denial reasons now embed the rule's scope (`rule:command:ping*`), and DENY rules match wrapped and chained commands: `cmd /c "ping x"` sailed past a `ping*` rule and was only caught by the dangerous-command confirm layer -- deny matching now unwraps cmd /c, cmd /k, powershell/pwsh -Command and sh/bash -c prefixes and checks each quote-blanked `&`/`;`/`|` segment (ALLOW rules deliberately stay on the plain command: widening deny fails closed, widening allow fails open). 32 new tests, suite 1101→1133. 工具类别轴 + 子 agent 权限栈传播：每个工具声明 `ToolCategory`（read/write/execute/external，未声明默认 EXTERNAL）；矩阵新单元 plan×WRITE（先于路径检查，拦住无路径参数的 install_skill）、plan×EXTERNAL（MCP）、bypass×EXTERNAL；5 份漂移的工具名单统一为类别。in-process 子 agent 获得权限门：`ChildPermissionManager` 子视图共享父级规则/授权/写文件集（/deny /mode 实时生效），confirm 恒 None 安全拒绝（并发弹窗问题消解）；类别门读 pm.mode 而非 loop 标志——传播因此生效；plan 的 spawn 封条改为有门放行/无门禁用。实测另修三处：ask_user 弹窗被流式渲染淹没（opens_dialog 属性化）；有门子 agent 被拒后无限找绕路（no_ui:default_deny 纳入熔断）；会话规则不可溯源（拒绝理由带规则来源，圆括号格式）；复验再修两处：方括号来源撞 Rich 标记致 trace 崩溃（改圆括号+动态字段全转义）、子 agent 写 bat 裸执行真实绕过 deny 规则（写后执行检测补段首 token 检查+call/start 形态）；子 agent 熔断报告带本轮全部去重拒绝原因（根因在前）与遗留文件清单，rule 拒绝附带可照抄的移除命令与全局生效事实（防盲目重派、误诊、编造命令与孤儿文件）；拒绝理由内嵌规则 scope（rule:command:ping*）；deny 规则匹配包装与串联命令（解包 cmd /c、powershell -Command、sh -c 前缀 + 抹引号后逐段匹配，allow 规则不解包——扩大 deny 是收紧、扩大 allow 是放松）。32 个新测试，1101→1133。
- **Permission mode matrix** — new `PermissionMode` (`default` / `accept-edits` / `plan` / `bypass`) on `PermissionManager`, aligned with mewcode's modes.py. `accept-edits` auto-approves file writes (dangerous commands still prompt); `plan` becomes truly read-only (write tools disabled + write-form bash commands like redirects/mkdir/copy/move/del denied + spawn_agents initially disabled, later re-enabled with propagated permission stacks — see the tool-category entry); `bypass` auto-approves everything EXCEPT the safety floor: explicit deny rules, sensitive paths, and sensitive-file commands (`type .env`) hold in EVERY mode. Runtime switch via new `/mode` command (aliases accepted, bypass shows a warning); startup mode via `[security] approval_mode` (invalid values warn and fall back); `/plan` and the `exit_plan_mode` tool converge on `Application.set_permission_mode()`. The `exit_plan_mode` tool now requires USER approval (yes/no question) — the LLM cannot lift its own read-only restriction; rejection keeps plan mode. Denial messages now carry the reason (`Permission denied for bash (reason: mode:plan — ...)`) so the LLM adapts instead of hunting configs. Observability: `PermissionModeChangedEvent` (trace `mode` line), `/status` shows the mode, the bottom toolbar always shows `mode: xxx`. Quote-aware write detection (`findstr ">" f` is a read); `cmd /c` joined the inline-interpreter dangerous list (27→28 patterns) — the Windows `sh -c` equivalent was missing. 31 new tests, suite 1070→1101. 权限模式矩阵：`default`/`accept-edits`（写免确认，危险命令仍询问）/`plan`（真只读：写工具禁用 + bash 写形态命令拒绝；spawn 初版禁用、后随权限栈传播恢复）/`bypass`（除安全底线全免确认）；deny 规则、敏感路径、敏感文件命令在所有模式下有效。`/mode` 运行时切换，`[security] approval_mode` 设启动模式，`exit_plan_mode` 需用户批准计划（LLM 不能自批解除只读）。拒绝消息带原因；模式切换事件 + /status + 工具栏可观测。引号感知写检测；`cmd /c` 补进内联解释器危险清单（27→28 条）。31 个新测试，1070→1101。
- **Background agent results auto-deliver** — a completed background agent's mailbox result no longer waits for the user's next input: `SubAgentCompleteEvent` triggers `terminal.interrupt_input()` (TTY: `prompt_session.app.exit()` with partial-input save/restore; non-TTY: `asyncio.wait` race), the main loop receives a sentinel and runs `_handle_background_delivery()` — a `while mailbox.has_pending("main")` loop so agents finishing during processing are never lost. New lockless `Mailbox.has_pending()`; `/spawn --background` dispatches from the command line. 3 new tests, 1063→1066. 后台 agent 结果自动投递：完成即中断输入等待、drain mailbox、触发 agent loop 处理（while has_pending 循环防处理期间完成的 agent 丢失）；新增 `/spawn --background`。3 个新测试，1063→1066。
- **User input line styling** — typed text renders bold bright light-blue (`theme.user_input`: default `#5fd7ff` / dark `#7dcfff` / light `#0969da`) via the prompt root style, framed by same-color rules above and below the input line — your own inputs are instantly findable in scrollback. 用户输入行醒目化：输入文字 bold 亮浅蓝（prompt 根样式）+ 上下同色横线，滚动历史中一眼定位自己的输入。

- **`-p` non-interactive one-shot mode + NDJSON event stream** — mini could not be driven by scripts/CI/pipes (interactive TUI, --remote and --worker were the only entry forms). New: `mini -p "task"` runs one prompt and exits (exit code 0 ok / 1 error); `--output-format stream-json` emits an NDJSON event stream — one JSON object per line, event names and payload shapes shared with the remote-mode WebSocket protocol (user_message/turn_start/stream_*/thinking_delta/tool_call/tool_result/turn_end/error/info/file_changes) so both machine interfaces parse the same way. stdout carries ONLY the result (all terminal/Rich output is redirected to stderr). Honest boundaries: actions that would need confirmation are denied fail-safe (`no_ui:default_deny`, breaker stops the loop) — relax via `[security] approval_mode`; the one-shot session is NOT persisted; SESSION hooks / memory extraction are skipped. 8 new tests, suite 1193→1201. `-p` 非交互一次性模式 + NDJSON 事件流：`mini -p "任务"` 执行后退出（退出码 0/1）；`--output-format stream-json` 逐行 NDJSON，事件命名与远程 WS 协议一致；stdout 只承载结果（终端输出整体转 stderr）。诚实边界：需确认操作失败安全拒绝（approval_mode 可放宽）、一次性会话不落盘、不跑 SESSION hooks/记忆提取。8 个新测试，1193→1201。

- **Comparison-doc factual corrections** — three stale claims in the mewcode comparison doc fixed after re-verifying against the mewcode source: ① remote auth: mini has token authentication (`--remote-token`, constant-time compare) while mewcode's remote.py has NO TLS/auth at all (its 4 "token" grep hits are LLM token counting) — the doc framed "no TLS" as a mini weakness without noting mini leads on auth; ② mewcode hook actions: "four action types" was wrong — the agent executor is a stub ("not yet implemented"), so 3 usable + 1 stub; also honestly downgraded mini's "EventBus covers observational hooks" argument to half-valid (capability reachable but requires Python vs mewcode's zero-code YAML + condition engine); ③ team-system size: "mewcode 13 files vs mini 3" was stale — measured mewcode teams/ 15 files 2069 lines vs mini 8 files 2055 lines, effectively at parity. 对照文档三处事实修正（实施前对 mewcode 源码逐条复验）：① 远程认证——mini 有 token 认证 mewcode 两者皆无，认证维度反超；② hook 动作"四种"失实——agent executor 是 stub，实为三种可用，并诚实降级 EventBus 论证为半成立；③ 团队文件数——实测 mewcode 15 文件 2069 行 vs mini 8 文件 2055 行，体量持平。

- **/spawn defaults to background auto-delivery** — `/spawn <task>` used to dispatch in "foreground": the result sat silently in memory until the user typed `/spawn wait`. Auto-delivery (`--background`) already existed but was opt-in — the two modes overlapped, foreground offering nothing except the extra manual step. The default is now inverted: no-flag `/spawn` (and `-p` parallel) auto-delivers results on completion (interrupts input wait, drains mailbox, triggers the agent loop); `--wait` remains the blocking opt-in (progress board + full formatted inline result); `--background` stays as a no-op alias for backward compatibility; `--pane` and the `wait`/`list`/`cancel` subcommands are unchanged. Honest boundary: auto-delivered results are LLM-relayed and truncated at 4000 chars — use `--wait` when you need the full output. 4 new tests, suite 1189→1193. /spawn 默认后台自动投递：原默认前台模式结果静躺内存需手动 /spawn wait，与已实现的 --background 自动投递功能重叠。现语义反转：无 flag 即自动投递（含 -p 并行）；--wait 保持阻塞式 opt-in（进度面板+完整内联结果）；--background 变 no-op 别名向后兼容；--pane 与子命令不变。诚实边界：自动投递结果经 LLM 转述且 4000 字符截断，要完整输出用 --wait。4 个新测试，1189→1193。

- **Ambiguous confirmation ≠ authorization + sub-agent anti-hallucination** — two system-prompt behavioral rules: ① When the user explicitly says "let's just discuss" / "don't make changes", ambiguous confirmations ("对"/"嗯"/"好"/"right"/"ok") only acknowledge understanding — they do NOT lift the constraint; the agent must ask before proceeding. Prompted by a real incident where the agent modified 6 files after a "对" that was part of continuing discussion. ② Sub-agents spawned WITHOUT inherited context now receive an explicit warning NOT to fabricate discussions/decisions they have no knowledge of — prompted by a sub-agent that confidently fabricated a complete plan with nonexistent filenames. Honest boundary: prompt rules are hints, not enforcement — weaker models may still violate. 模糊确认不算授权 + 子 agent 反幻觉守则：① SYSTEM_PROMPT 新增规则——用户明确表示只讨论时约束持续有效直到显式动手指令，模糊确认只确认理解不解除约束；② 无继承上下文的子 agent 收到引用未知讨论的任务时须如实说明，不得编造。诚实边界：prompt 守则是提示非强制。

- **/session list pagination** — with 150+ saved sessions, `/session list` dumped every row and flushed the terminal scrollback (observed with 159 sessions in real use). Now shows the latest 20 by default (constant `_SESSION_LIST_LIMIT`, not configurable — `--all` is the escape hatch); real pagination via `--page N` (rows 21-40 on page 2, etc.) with a bilingual footer `Page N/M (total) — --page N+1 for next / --all for everything` (no next-page hint on the last page; out-of-range pages report the page count); `--all` takes precedence over `--page`; `--tag` filtering combines freely with all of it. 7 new tests, suite 1182→1189. /session list 分页：150+ 会话时整屏刷过（实测 159 个）。默认只显示最近 20 条（常量不进配置，--all 即出口）；`--page N` 真分页（第 2 页显示第 21~40 条），尾行双语提示页码/总数/下一页与 --all 用法（末页无下一页提示，页码超范围报错并提示总页数）；--all 优先于 --page；--tag 过滤与全部参数自由组合。7 个新测试，1182→1189。

- **Crashed session cleanup** — crashed/hard-killed sessions (`closed_cleanly=False`) were never cleaned up by `SessionStore.cleanup_stale()` and accumulated permanently on disk. `cleanup_stale()` now accepts a `crashed_max_age_days` parameter (default 40 days, configured via `MemoryConfig.crashed_session_cleanup_days`, 0 = keep forever); crashed sessions older than this are deleted at startup BEFORE crash-recovery detection — 40-day-old crashes are not worth auto-restoring. 40 days is intentionally longer than the 30-day normal-session cutoff (crash sessions have recovery value). `config.toml.example` now documents both `[memory]` cleanup knobs. 3 new tests, suite 1179→1182. 崩溃会话启动清理：崩溃/硬杀会话之前永久留盘不被清理。`cleanup_stale` 新增 `crashed_max_age_days` 参数（默认 40 天，`MemoryConfig.crashed_session_cleanup_days` 配置，0 永久保留）；超龄崩溃会话在崩溃恢复检测之前被删——40 天前的崩溃不值得自动恢复。40 天比正常 30 天更宽松（崩溃会话有恢复价值）。`config.toml.example` 补全两个清理配置文档。3 个新测试，1179→1182。

- **Remote mode session persistence** — remote/browser mode (`--remote`) ran `server.start()` instead of `Application.run()`, bypassing ALL session persistence: no per-turn autosave, no crash restore, no clean-shutdown marker — a server restart lost every conversation (an acknowledged limitation). The store and helpers already existed UI-free on Application; the server now wires them in: per-turn force-save, throttled save after slash commands, `cleanup_stale` + AUTO-restore of this project's newest unclean session at startup (no prompt — no client is connected yet to ask; terminal mode keeps its prompt-based restore via the extracted shared `_find_crashed_session()` helper), and a clean-shutdown save in `start()`'s finally. Also fixes a pre-existing blind spot: `/session load`/`/fork` swapped the conversation without telling the browser — a new `history_reset` WS event now clears the chat area and replays the adopted history to all clients. Honest boundary: auto-restore only covers crash/hard-kill; a clean Ctrl+C restart starts fresh (use /session load to resume). Re-verification of the unwanted-restore workflow exposed and fixed two more holes: NEW `/session new` subcommand — the only safe fresh-start (a bare `/clear` keeps the session ID, so autosave overwrites the old history on disk — a pre-existing terminal-mode footgun, now documented); and a pre-existing stale-state bug — adopting a boundary-less session inherited the previous session's read-file cache and skill state (new `ContextManager.reset_state()`, called before every adoption). 13 new tests, suite 1166→1179. 远程模式会话持久化：远程模式跑 server.start() 绕过全部持久化——重启丢会话（已承认限制）。现接入四点：每轮强制保存/斜杠后节流保存/启动 cleanup+自动恢复本项目最新未关闭会话（不询问——启动时无客户端；终端保持询问式，共用提取的 _find_crashed_session 助手）/退出 finally 标记正常关闭并保存。附带修复既有盲区：/session load 换会话后浏览器不知情——新 history_reset 事件清空聊天区并重放。诚实边界：自动恢复只针对崩溃/硬杀，正常关闭重启从新会话开始。误恢复处置复验再补两洞：新增 /session new 安全另起（裸 /clear 不换会话 ID、自动保存会覆盖盘上旧历史——终端既有坑，本次文档化）；修复采用无边界会话继承上一会话已读文件缓存与技能状态的既有陈旧状态 bug（ContextManager.reset_state()，每次采用前复位）。13 个新测试，1166→1179。

- **On/off mode commands: no-arg = show status** — the 4 on/off mode commands (`/trace`, `/explain`, `/audit`, `/plan`) had inconsistent no-argument behavior: `/trace`/`/explain`/`/audit` toggled, `/plan` unconditionally turned on. Now all four show the current state without changing it when called with no arguments; `on`/`off` explicitly switch. 4 new tests, suite 1162→1166. on/off 模式命令无参数只显示状态：4 个 on/off 命令（/trace /explain /audit /plan）无参数行为各不相同（前三个 toggle，/plan 无条件打开）。统一为无参数只显示当前状态不改变，on/off 显式切换。4 个新测试，1162→1166。

### Fixed 修复

- **Synchronous file I/O inside async methods moved off the event loop** — all file tools (read_file/write_file/edit_file/grep/glob) and SessionStore called blocking `Path.read_text()`/`write_text()` directly inside `async def` methods, freezing the event loop (streaming, ESC watching, concurrent tasks) for the whole scan — measured 1.9s total freeze on a 3000-file/70MB grep. All blocking I/O now goes through stdlib `asyncio.to_thread` (no new dependency): traversal loops (grep/glob scan, SessionStore list/cleanup) are extracted into sync methods and offloaded as a whole (one thread hop instead of per-file); single-file calls are wrapped individually (write's mkdir+write, save's dumps+write, load's read+parse packed into closures). Single metadata pre-checks (`is_file`/`stat`) stay on the loop — offload cost exceeds the win. Measured after: max loop stall 44ms during the same scan, ~12% scan-time overhead. 4 new deterministic regression tests (threading.Event proof, no timing flakiness), suite 1434→1438. Fixes #271. 异步方法内的同步文件 I/O 移出事件循环：所有文件工具与 SessionStore 在 `async def` 里直接调用阻塞的 read_text/write_text，扫描期间事件循环全程冻结（流式输出/ESC 监听/并发任务停摆）——3000 文件 70MB 的 grep 实测冻结 1.9s。全部阻塞 I/O 改经 stdlib `asyncio.to_thread`（零新依赖）：遍历型循环（grep/glob 扫描、SessionStore 列表/清理）提取为同步方法整体下放（一次线程切换而非逐文件）；单文件调用逐个包装（write 的 mkdir+写、save 的序列化+写、load 的读+解析各打包闭包）。单次元数据前置检查留在循环上——下放成本高于收益。修复后同规模扫描最大冻结 44ms，扫描耗时增约 12%。4 个确定性回归测试（threading.Event 证明，无计时 flaky），1434→1438。修复 #271。

- **Thinking-stream fragmented lines** — reasoning models' `reasoning_content` deltas rendered as broken fragments before the answer (most thinking text vanished, stray pieces like `.`/`11` each on their own line). Root cause (found after a first `soft_wrap=True` fix failed real-run verification): agent_loop starts the StreamRenderer's Live on the FIRST thinking chunk, and `console.print` during an active Live is intercepted — each tiny delta becomes its own line block (`end=""` is lost) followed by Live's `\r\x1b[2K` erase-line refresh, so fragments get erased/broken. Fix: Live start is deferred to the first ANSWER delta (`feed_stream`); thinking deltas write directly to the terminal (soft_wrap kept for the no-Live wrap path); the dangling thinking line is closed before the answer starts. ANSI-level verification with `force_terminal=True`: old behavior shows per-fragment erase codes, new behavior streams thinking as one continuous dim line. 4 new tests, suite 1158→1162. 思考流碎行修复：真因（第一次 soft_wrap 修复真实运行验证失败后重查）是 agent_loop 在首个 thinking chunk 就启动 Live，Live 活跃期间每次 print 被拦截为独立行块（end="" 失效）且每碎片后跟 `\r\x1b[2K` 整行擦除——碎片被擦除/打断只剩零星孤行。修复：Live 延迟到首个正文 delta 才启动，思考期间直连顺序写入（soft_wrap 保留管无 Live 路径折行），正文开始前收尾思考行。force_terminal=True ANSI 级验证：旧行为每碎片带擦除码，新行为思考文本连续完整。4 个新测试，1158→1162。

- **Confirm-dialog input line protected from concurrent output** — when parallel tools finished while a permission dialog waited for input, their trace lines printed INTO the `allow? [y/a/n] >` line. New `Terminal._prompt_protected()` wraps the dialog prompts (`confirm`/`ask_yes_no`/`ask_structured`) in `patch_stdout` so concurrent output reroutes above the prompt; falls back to a bare prompt when the stdout proxy cannot be built (no console). 4 new tests, 1066→1070. 确认弹窗输入行防并发输出打断：三条弹窗输入路径包进 patch_stdout，并发输出重定向到提示行上方；无控制台环境退回裸 prompt。4 个新测试，1066→1070。
- **Fork summary generation is now observable and non-blocking** — `inherit_context=true` triggered a full LLM summarization call (46-54s) inside `spawn_agents.execute()` with zero terminal output and zero trace events — the user thought the terminal was frozen. ① Observable: `build_context_summary()` now emits `ContextSummaryStartEvent`/`ContextSummaryDoneEvent`; app.py subscribes to show terminal hints ("Summarizing conversation for context fork..." / "Context summary ready"), TraceRenderer subscribes to show `ctx` trace lines under `/trace on`. ② Non-blocking: `background=true + inherit_context=true` now defers summary+spawn to a background `asyncio.Task` (`execute()` returns instantly); message list is shallow-copied to prevent mutation; task reference stored in `_notify_tasks` to prevent GC. 3 new tests, suite 1060→1063. fork 摘要生成可观测+非阻塞：`inherit_context=true` 的 LLM 摘要调用（46-54s）期间终端零输出、`/trace on` 也不可见——用户以为卡死。① 可观测：`build_context_summary()` 前后发射 `ContextSummaryStartEvent`/`ContextSummaryDoneEvent`，app.py 订阅显示终端提示，TraceRenderer 订阅在 `/trace on` 下显示 `ctx` 行。② 非阻塞：`background=true + inherit_context=true` 时摘要+spawn 整体放进后台 `asyncio.Task`（`execute()` 立即返回），消息列表浅拷贝防竞态。3 个新测试，1060→1063。
- **Sensitive-file read via bash bypassed the read_file DENY (API key leaked)** — real-run finding during verification: `read_file` correctly denied `.env` (a sensitive-file match), but the agent then ran `type D:\...\.env` through the bash tool and printed the file, leaking a real API key. Root cause: sensitive-file protection (`PathGuard.is_sensitive_file`) only guards the `read_file`/`write_file`/`delete_file` tools — the bash command pipeline never checked paths, so `type`/`cat`/`Get-Content`/`more .env` sailed through as normal commands. Fix: new `command_references_sensitive_file()` in permission.py tokenizes a shell command (whitespace/`=`/shell operators) and flags any token whose basename matches `SENSITIVE_FILE_PATTERNS` (`.env`, `*.pem`, `*.key`, `id_rsa*`, `credentials*`, `*secret*`, …; `.env.example`/`.sample`/`.template` excepted). Such a command now routes to `_ask_user` with reason `sensitive_file_command` — a confirm, not a silent auto-deny, so it is visible AND (via `user_confirm:no`) trips the confirm-denial breaker on denial. A speed bump, not a wall: obfuscated paths (env vars, wildcards, base64) can still slip through — same honest boundary as the command blacklist. 2 new tests, suite 1058→1060. bash 读敏感文件绕过 read_file 拦截（泄漏 API key）：真实验证发现——read_file 正确拒了 `.env`，但 agent 改用 `type .env`（bash 通道）成功打印、泄漏真实 API key。根因：敏感文件保护只在文件工具层，bash 命令管道从不查路径。修复：新增 `command_references_sensitive_file()` 切分命令 token、命中敏感文件名即路由到确认（reason=`sensitive_file_command`），用确认而非静默拒绝——可见且拒绝时触发确认拒绝熔断停目标。减速带非围墙：变量/通配/base64 等混淆仍可逃逸，同命令黑名单诚实边界。2 个新测试，1058→1060。
- **Deletion detection broadened to any form + `rd` alias** — the rm/del/rmdir patterns only matched aggressive forms (`rm -rf`, `del /q`, `rmdir /s`), so a bare `rmdir dir`, `del file`, or `rm file` was silently auto-allowed; the Windows `rd` alias was not covered at all. The three patterns now catch every deletion form (bare `rmdir` on empty dirs and `rm`/`del` on a single file included) and a new `rd` pattern was added, 26→27 patterns. 2 new tests. 删除类命令检测放宽到任意形态 + 新增 `rd`：原 rm/del/rmdir 正则只匹配激进形态（rm -rf、del /q、rmdir /s），裸 `rmdir 目录`、`del 文件`、`rm 文件` 会被静默放行；Windows 别名 `rd` 完全未覆盖。现三条正则命中任意删除形态（含裸 rmdir 删空目录、rm/del 删单个文件），并新增 `rd` 模式，26→27 条。2 个新测试。
- **Confirm-denial circuit breaker — stop and ask instead of hunting for bypasses** — behavior-layer fix for the same proven incident the inline-interpreter work patched at the execution layer: the LLM was denied 4 dangerous delete commands in a row (rm/rmdir/del variants) yet kept going, and on iteration 12 found a bypass that deleted the directory. Now `AgentState.consecutive_confirm_denials` counts every confirm-dialog denial — dangerous command, path outside project, or hook (`[[hooks]] action=confirm`) — keyed on permission reason `user_confirm:no` plus hook-confirm denials (the interim `PermissionManager.last_check_was_dangerous` peek attribute was removed). A denial increments, a confirmed grant resets to 0, calls that never prompted are neutral (read-only analysis between denials doesn't reset it), and the counter resets each turn. Auto-policy denials do NOT count — sensitive-path blocks (`path_guard:sensitive`), explicit deny rules, and the no-UI default-deny still just skip that one call and continue. When the counter reaches `AgentConfig.max_consecutive_denials` (new config field, default 1 — denying a single confirm stops the whole goal immediately; raise it to give the agent room to retry after a denial), `_should_continue()` sets `stop_reason="confirm_denied"` and stops the loop; the agent returns a message asking the user how to proceed, and app.py shows a distinct warning. Complements the execution-layer work: that makes bypass paths also prompt, this makes the agent stop trying after refusals. Suite total after breaker + deletion-detection work: 1058. 确认拒绝熔断：任何确认框被拒即熔断（危险命令/项目外路径/hook 确认，以 `user_confirm:no` + hook 确认被拒为准；默认阈值 1，`max_consecutive_denials` 可调大给被拒后修正重试的空间），Agent 停下回问用户，不再继续寻找绕过路径——与执行层封堵内联解释器绕过互补的行为层修复。自动策略拒绝不计数：敏感路径拒绝、显式 deny 规则、无 UI 默认拒绝仍只是跳过该次调用、任务继续。计数器每回合重置、确认获准即清零、未弹确认的调用中性不重置；触发时 `stop_reason="confirm_denied"`，app.py 显示独立警告。熔断 + 删除检测后测试共 1058。
- **Inline interpreter bypass blocked + Windows Low Integrity sandbox** — fix for the proven bypass (LLM denied `rm -rf` → ran `python -c "shutil.rmtree(...)"` → GRANTED → directory deleted). ① `DANGEROUS_COMMAND_PATTERNS` gained 7 inline-interpreter regexes, 19→26 patterns; all trigger confirmation. ② New `sandbox/windows.py` WindowsSandbox: admin → Low Integrity process (kernel-enforced via `_low_integrity.py` ctypes helper, equivalent to bwrap/seatbelt); non-admin → no file protection, no startup warning (attrib approach disabled — it blocked agent's own writes to session/history/memory; limitation documented in config-guide). ③ Write-then-execute detection: `record_written_file()` + `is_executing_written_script()` catch `python script.py`/`cmd /c`/`python -m`/`python - <`. ④ `sandbox` default true. ⑤ Linux unshare fallback when bwrap unavailable. ⑥ Startup warning when sandbox degraded. ⑦ `_ps_escape` handles `/$. ⑧ `working_dir` + `shared_written_files` for cross-agent tracking. 1055 tests. 31 known residual items in roadmap. 内联解释器绕过封堵 + Windows Low Integrity 沙箱。管理员→Low Integrity（内核级，ctypes helper，等同 bwrap/seatbelt）；非管理员→无文件保护、不打启动警告（attrib 已禁用——会阻断 agent 自身写入 session/history/memory；限制仅 config-guide 文档说明）。写后执行检测（record_written_file + is_executing_written_script）。sandbox 默认开启。Linux unshare 后备。后端不可用时启动警告。1055 测试。31 项遗留见 roadmap。
- **Fork-style context inheritance for sub-agents** — sub-agents are born with blank context by design, but tasks like "implement what we discussed" need the discussion. New: `spawn_agents` tool's `inherit_context=true` (LLM-initiated) and `/spawn --fork` (user command) inject an LLM-generated summary of the parent conversation into each sub-agent's system prompt (`[Inherited context ...]` section). Summary is a frozen snapshot (avoids fork-consistency issues) generated once per spawn call via the new public `summarize_conversation()` in compressor.py (reuses P67's 9-section structured summary; falls back to the extractive digest on LLM failure — a fork never fails because summarization failed). `context_summary` threads through spawn/spawn_parallel/spawn_background; composes with `background=true`. 6 new tests. Out of scope: spawn_pane (cross-process protocol) and /team. 摘要式上下文 fork：spawn_agents 的 inherit_context=true 与 /spawn --fork 把父对话的 LLM 摘要注入子 agent system prompt（冻结快照，成本一次摘要调用）；复用 P67 摘要能力，LLM 失败回退提取式 digest；与 background 模式可组合；6 个新测试；spawn_pane 与 /team 未纳入。
- **Background sub-agents + completion notification** — the LLM's `spawn_agents` tool no longer has to block: `background=true` returns immediately with agent ids so the LLM continues other work; each agent's result (truncated to 4000 chars) is delivered to 'main' via the existing mailbox channel when it completes, and the main agent auto-injects it on its next iteration (`_deliver_mail`). New `SubAgentManager.spawn_background()` + `_notify_on_complete()` notifier coroutines; `SubAgentCompleteEvent` gains a `background` field; app.py subscribes to show a terminal hint on completion. Default remains blocking (backward compat). 5 new tests. The idle-delivery limitation noted at the time was later removed by the background auto-delivery entry above. 后台子代理 + 完成通知：spawn_agents 加 background=true 立即返回，LLM 可继续其他工作；子 agent 完成后经 mailbox 通知 main、下一轮迭代自动注入对话（复用现有通道零新机制）；SubAgentCompleteEvent 加 background 字段 + 终端完成提示；默认仍阻塞；5 个新测试；当时记录的空闲滞留限制后续已由上方"后台结果自动投递"条目解除。
- **/spawn + /trace prompt buried fix** — background sub-agent trace output (shared EventBus → shared Rich Console) overwrote the `>` input prompt during `prompt_async`, making the terminal appear stuck. Fix: `patch_stdout=True` on `PromptSession` — prompt_toolkit's built-in mechanism prints async stdout above the active prompt and redraws it. One-line change. /spawn + /trace 提示符被淹没修复：后台子 agent 的 trace 输出覆盖输入提示符，用户以为终端卡住。修复：PromptSession 加 patch_stdout=True，异步输出自动打印到 prompt 上方并重绘。一行改动。
- **Custom agent types from .md files** — users can now define custom agent types by placing `.md` files in `.mini-agent/agents/` (project-level) or `~/.mini-agent/agents/` (user-level). Each file uses YAML frontmatter (`name`, `description`, `allowed_tools`, `max_iterations`) with the body as the system prompt template (supports `{working_dir}`, `{platform}`, `{shell}`, `{iteration_budget}` placeholders). Custom types are available via `/spawn --type <name>` and `spawn_agents` tool. Priority: project > user > builtin; same-name custom types override builtins. `spawn_agents` tool schema now dynamically lists all registered types. New `agent_type_loader.py` (~100 lines), `AgentConfig.agent_dirs` config field, 12 new tests. 自定义 Agent 类型：用户可在 `.mini-agent/agents/` 或 `~/.mini-agent/agents/` 放 `.md` 文件声明自定义类型（YAML frontmatter 元数据 + body 系统提示词模板）。支持 `/spawn --type` 和 LLM 自主选择。优先级：项目 > 用户 > 内置，同名覆盖。spawn_agents 工具 schema 动态列举所有类型。新增 agent_type_loader.py、agent_dirs 配置项、12 个新测试。
- **Forced read-before-edit made configurable** — the read-before-edit gate was wired unconditionally: app.py/sub-agents always created a FileStateCache, so users could not turn enforcement off (an `enforce_read_before_edit` config field existed but was never read — dead config). Fix: `[tools] enforce_read_before_edit` (default `true`) now actually gates the wiring in both the main agent and every sub-agent; `false` sets `file_state=None` and disables the gate entirely. 5 new tests (config default, app wiring on/off, sub-agent wiring on/off). Documented in config.toml.example + config guides (zh/en). 强制 read-before-edit 改为可配置：原先门禁无条件接线、配置字段是从未被读取的死代码；现 `[tools] enforce_read_before_edit`（默认 true）真实控制主 Agent 与所有 SubAgent 的装配，false 时 file_state=None 门禁完全关闭；5 个新测试；配置示例与中英配置指南同步。
- **Read-before-edit enforcement (FileStateCache)** — edit_file/write_file now refuse to modify a file that was never read, or that changed externally since the last read (stale-content guard). New `tools/file_state_cache.py`: session-scoped {abs_path: mtime_ns} cache, two gates (must-have-read + mtime-unchanged). read_file records; edit_file checks before + updates after; write_file requires a prior read only when overwriting an existing file (new files exempt); delete_file not gated. ToolContext gains `file_state` (main agent and each sub-agent hold independent caches); None disables the gate (backward compat). 10 new tests. Real-LLM verified: a direct edit is blocked, and the LLM self-corrects to read-then-edit. 编辑前必读强制：edit/write 拒绝修改未读过或读后被外部改动的文件；新增 FileStateCache（两道门：必须读过 + mtime 未变）；read 记录、edit 前检查后刷新、write 仅覆盖已存在文件时要求先读、delete 不纳入；ToolContext 加 file_state（主 Agent 与各子 Agent 独立缓存）；10 个新测试；真实 LLM 验证直接编辑被拦、LLM 自主改为先读再编辑。
- **Skill tools: load_skill + install_skill** — LLM can now activate installed skills (`load_skill`) and install new ones from local paths or git URLs (`install_skill`). ToolContext extended with `skill_registry`; no permission confirmation needed (visible in terminal, `/deny tool install_skill` to block). 6 new tests. Built-in tool count 18 → 20. 技能工具：LLM 可自主激活已安装技能（load_skill）和从路径/git URL 安装新技能（install_skill）；ToolContext 扩展 skill_registry；不弹权限（终端可见，/deny 可禁）；6 个新测试；工具总数 18→20。
- **6 LLM-invokable process tools** — ask_user (structured question with choices or free text, Rich Panel terminal UI), exit_plan_mode (LLM signals plan complete, exits plan mode for user review), task_create/task_get/task_list/task_update (persistent task board CRUD exposed to LLM, reusing TaskStore). ToolContext extended with task_store/agent_loop_ref/ask_user_callback; app.py wires them at startup. Built-in tool count 12 → 18. 14 new tests + integration assertion updated. Real-LLM verified (task_create invoked autonomously). Worktree/team tools deferred (skill tools added in a subsequent batch). 核心批次：6 个 LLM 可自主调用的流程工具——ask_user（结构化提问）、exit_plan_mode（计划审批闭环）、task CRUD ×4（任务板暴露给 LLM）。ToolContext 扩展 3 字段，app.py 装配注入。工具总数 12→18，14 个新测试，真实 LLM 验证通过。
- **Spill-readback misjudgment fixed: path-component containment instead of string prefix** — `is_spill_readback` used `abs_path.startswith(abs(cache_dir))`, so a sibling dir `.../cache_evil/x` (cache_dir `.../cache`) matched as a string prefix and was wrongly exempted from spilling. Fix: use `Path.resolve()` + `resolved == cache_root or cache_root in resolved.parents` (same pattern as PathGuard's spill read-only allow); removed the now-unused `os` import. Regression test asserts a sibling dir does not match while the real cache dir still does. 溢写读回误判修复：`is_spill_readback` 的字符串前缀匹配会把兄弟目录 `.../cache_evil/x` 误判为读回；改用路径成分包含（`Path.resolve()` + `in parents`），回归测试验证兄弟目录不命中、真缓存目录仍命中。
- **Remote mode: fix 404 when token is in URL query** — `_process_http` compared `request.path == "/"` but websockets includes the query string in `path` (e.g. `/?token=xxx`), so the HTML page returned 404 whenever `--remote-token` was used. Fix: split off query before comparing. 远程模式修复：带 `--remote-token` 时浏览器访问返回 404——`request.path` 含查询字符串（`/?token=xxx`），`== "/"` 永远不匹配。修复：比较前去掉查询字符串。
- **Remote mode hardened: constant-time token comparison + security warning** — token authentication in `remote/server.py` now uses `hmac.compare_digest` instead of `!=` (eliminates timing side-channel); startup output warns that the token appears in the URL (browser history / server logs / proxy logs exposure) and recommends a reverse proxy with TLS for public exposure. Regression test verifies `compare_digest` is present in handler source. TLS and per-client session isolation are intentionally not addressed (reverse proxy is the correct layer for TLS; shared session is by design). Remote 模式加固：token 认证改用 `hmac.compare_digest` 消除时序侧信道；启动输出加安全提醒（token 在 URL 中可能泄露，建议 reverse proxy + TLS）；回归测试验证 compare_digest 存在；TLS 与多客户端隔离有意保留（reverse proxy 是正确层、共享会话是设计意图）。
- **Concurrency fixed: CostTracker now lock-guards concurrent token accumulation** — `_on_response` read-modify-writes `self.usage[model]` under `asyncio.Lock`, preventing lost updates when parallel SubAgents emit `LLMResponseEvent` on the shared EventBus (token/cost undercount, budget fuse misfire). Only `_on_response` is locked (the concurrent entry point); `end_turn`/`flush_to_ledger` run single-threaded in the main loop. Regression test fires 200 concurrent events and asserts exact sums. 并发修复：CostTracker 的 `_on_response` 对 `self.usage` 的读-改-写加 `asyncio.Lock` 保护，防止并行子 Agent 经共享 EventBus 并发写入时丢失更新（token/成本少计、预算熔断失准）；只锁 `_on_response`（并发入口），`end_turn`/`flush` 在主循环单线程调用无需锁；回归测试 200 并发事件断言精确求和。
- **Fail-open timing fixed: write tools deferred from mid-stream eager execution** — with streaming tool execution, a write/delete tool call that finished assembling before a truncated response (finish_reason="length") was detected would execute eagerly; the max_tokens-recovery retry then re-issued the same call, running it twice (double write / double delete) since `task.cancel()` cannot roll back a completed side effect. Fix: `_WRITE_TOOLS` (write_file/edit_file/delete_file) are never eager-executed mid-stream -- they defer to `_act`, which runs only after recovery settles on a final non-truncated response. Regression test asserts a single `execute()` call across a truncate-then-retry sequence. Residual (documented): side-effecting non-dangerous bash can still double-run on truncation; dangerous bash is already deferred via would_ask. Fail-open 时序修复：流式期间抢先执行的写/删工具在截断重试后会双执行（cancel 无法回滚已完成副作用）；修复将 write_file/edit_file/delete_file 延迟到 _act（恢复确定后才执行），回归测试断言截断-重试序列中 execute() 仅一次。残留：带副作用的非危险 bash 仍可能双跑（危险 bash 已由 would_ask 延迟）。
- **Dangerous-command regexes hardened: no longer bypassed by option shapes** — the 19 `DANGEROUS_COMMAND_PATTERNS` could be evaded by trivial variants (auto-allowed silently in ask/allow mode): `rm --recursive --force`/`rm foo -rf` (long options / trailing flags), `git -C /repo push` (global option between git and subcommand), `chmod -R 777` (leading flag). Fix: rm tolerates long options and flags in any position; chmod tolerates leading flags and 0777; new `_GIT_PREFIX` swallows git global options (-c/-C with values + attached form) for all 7 dangerous subcommands. Two new tests (bypass variants flagged + safe commands not flagged); real-run verified `chmod -R 777` and `git -C . push` now prompt. Honest boundary: a regex blacklist can never be exhaustive (an LLM can reshape commands to evade signatures — proven by the deadlock experiment); this is a speed bump, not a wall — the iteration limit and human-in-the-loop confirm are the real safeguards. 加固:19 条危险命令正则曾被简单变形绕过(ask/allow 模式静默放行):rm 长选项/标志后置、git 全局选项插入、chmod 加 -R。修复:rm 容忍任意位置标志与长选项、chmod 容忍前置选项与 0777、新增 _GIT_PREFIX 吞掉 git 全局选项覆盖 7 个危险子命令;两个新测试(绕过命中 + 安全零误报);真实运行验证 chmod -R 777 与 git -C . push 现在弹确认框。诚实边界:正则黑名单不可能穷尽(死循环实验已证 LLM 可变形绕过),只是减速带非围墙,迭代上限与人工确认才是真护栏。
- **Fail-open fixed: `delete_file` now routes through PathGuard** — `_check_permission` previously routed only read_file/glob/grep (read) and write_file/edit_file (write); delete_file fell into the unrestricted else branch and was unconditionally GRANTED, bypassing sensitive-path deny, project-boundary checks, and denied_paths. One-line fix: added delete_file to the write routing tuple. Regression test `test_delete_file_routes_through_path_check` verifies ~/.ssh/id_rsa is DENIED. spec.md routing diagram and non-write-step tool stripping lists updated. Fail-open 修复：delete_file 此前落入无约束 else 分支被无条件放行，敏感路径拒绝、项目外确认、denied_paths 全部失效。修复：加入 write 路由元组；回归测试验证 ~/.ssh/id_rsa 被拒；spec 路由图与非写步骤剥离列表同步更新。
- **Dead per-iteration loop fuse revived (found by the spec audit)** — guard 2 ("same tool present in each of the last 15 iterations") could never fire: `AgentState.record_iteration_tools` capped the sliding window at 8 entries while the guard required ≥15. Worse, its unit test passed for the wrong reason (MockLLM repeats its last script entry, so identical args tripped guard 1 instead — a lying test masking dead code). Window cap raised to 15; test rewritten with distinct args per round and now asserts the loop stops exactly at iteration 15. spec 对账审计挖出的死熔断修复：按轮熔断（连续 15 轮同工具）因滑窗上限 8 而永不可达；其测试靠 MockLLM 重复末条脚本误触护栏 1 假通过（撒谎测试掩盖死代码）。窗口上限提至 15，测试改为每轮不同参数并断言恰在第 15 轮熔断。

### Changed 变更

- **Test infra: 13 MockLLM copies deduplicated into `tests/mocks.py`** — the near-identical scripted MockLLM was independently defined in 13 test files (the assessment's 11 plus a nested copy in test_extension_points and a bare non-ABC copy in test_tool_categories), and 3 more files imported it from test_agent_loop — de-facto sharing parasitizing a test file. One shared MockLLM now covers all variants: `scripts` replay (last script repeats), `text=` shorthand, `delay`, `error` (exception instance, unifying RuntimeError/ConnectionError flags), public `call_count`; plus shared `text_response`/`tool_call_response` helpers. Specialized mocks (SummaryMockLLM/TeamMockLLM/memory `_MockLLM`) stay put — different behavior surface, not duplication. Behavior subclasses (KwargsMockLLM/YieldingMockLLM/TwoPhaseStreamLLM) now inherit the shared class. 16 files, −350/+49 lines; suite stays 1438, pure refactor. Fixes #273. 测试基础设施：13 份 MockLLM 复制收敛为 tests/mocks.py 单一实现——评估列出的 11 处外加 test_extension_points 嵌套版与 test_tool_categories 非 ABC 裸版，另有 3 个文件从 test_agent_loop 跨文件导入（事实共享寄生在测试文件里）。共享 MockLLM 覆盖全部变体：scripts 重放（末脚本重复）、text= 简写、delay、error（异常实例，统一 RuntimeError/ConnectionError 开关）、公开 call_count，附 text_response/tool_call_response 辅助。功能特化 mock（Summary/Team/memory）不并入——行为面不同非重复。行为子类改继承共享类。16 文件 −350/+49 行；1438 测试数量不变，纯重构。修复 #273。

- **Docs: spec.md fully reconciled with the current implementation** — five parallel audits compared every section (§2-§15 + architecture principles) against source and ~60 discrepancies were rewritten in place: real event list (14 events, 9 phantom ones removed), 12-tool table, triple-fuse loop guards, three-stage compression cascade with dual thresholds/circuit breaker/spill cache, permission scope dispatch, agent types/Mailbox/pane workers/remote mode added to the multi-agent section, 7-layer config stack, plugin ecosystem section, never-implemented TOML slash-command example deleted, the last 实现注记 absorbed into main text. spec.md is now a living document kept in sync. spec.md 全文对账：五路并行审计对照源码改写约 60 处失真——真实事件清单、12 工具表、三重熔断、三级压缩级联（双阈值/熔断器/溢写）、权限 scope 分发、多 Agent 章补类型系统/Mailbox/窗格 worker/远程模式、七层配置栈、插件生态节、删除未实现的 TOML 斜杠命令设计、吸收最后一处实现注记；spec.md 此后作为活文档持续同步。
- **Docs: pure-English versions of the four user guides (`docs/guide/en/`)** — full English translations of commands-guide / config-guide / output-guide / terminal-guide, cross-linked with the Chinese originals (each file's header points to its counterpart); the English README now links the English guides, README-zh keeps the Chinese ones. Both languages must be updated together from now on. 新增四个使用指南的纯英文版（docs/guide/en/），与中文版头部互指链接；英文 README 链英文版、README-zh 链中文版；此后中英两份须同步维护。
- **Docs restructure: `todo-code-quality.md` merged into `roadmap.md`** — full content moved verbatim as roadmap section 八 (94 check-marks / 91 bullets / 33 table rows preserved 1:1, headings demoted one level); the only content edit is the now-self-referential roadmap link. Doc count 15 → 14; live references updated (tech-notes link, spec/README-zh trees, capabilities/comparison-mewcode counts); historical logs keep original mentions. 文档合并：todo-code-quality.md 全文原样并入 roadmap 第八节（94 个 ✅ / 91 个列表项 / 33 个表格行 1:1 保留，标题整体降一级）；唯一内容改动是并入后自引用的 roadmap 链接改指本文档章节。文档数 15→14；活引用全部更新（tech-notes 链接、spec/README-zh 目录树、capabilities/comparison-mewcode 计数）；历史日志按惯例保留原提及。
- **Docs restructure: user guides moved to `docs/guide/` ** — `commands-guide.md`, `config-guide.md`, `output-guide.md`, `terminal-guide.md` moved from `docs/` into `docs/guide/`; all non-historical references (README ×2, config.toml.example, cross-links in docs/) updated, verified zero broken markdown links. Historical logs (tasks/checklist/CHANGELOG) keep original paths by convention. 文档重构：`commands-guide.md`、`config-guide.md`、`output-guide.md`、`terminal-guide.md` 四个使用指南从 `docs/` 移入 `docs/guide/`；全部非历史引用（README 双语、config.toml.example、docs 内互链）已更新并经链接检查器验证全库零断链；历史日志（tasks/checklist/CHANGELOG）按惯例保留原路径。

## v1.1.0

### Added 新增

- **Plugin ecosystem (P83)** — third-party pip packages and local `.py` files can now register tools, slash commands, and skills. New `extensions/plugin_loader.py`: four-hook contract (`register(ctx)` full-control takes precedence; `register_tools`/`register_commands`/`register_skills` specific hooks), dual discovery (`mini_agent.plugins` entry-point group + `plugin_dirs`, default `./.mini-agent/plugins` and `~/.mini-agent/plugins`), three-layer fault isolation (import/hook/runtime — broken plugins never break the agent), `disabled_plugins` config kill switch, `/plugins` command showing what each plugin registered. Plugin tools deliberately bypass the `enabled_tools` builtin whitelist (installing the plugin is the opt-in). `SkillRegistry.register()` keeps programmatic skills alive across `/skill reload`. Example plugin in `examples/plugins/word_count_plugin.py`. 16 new tests, 968 total; real-LLM verified (plugin tool invoked live). 插件生态：第三方 pip 包与本地 `.py` 文件均可注册工具、斜杠命令和技能。新增 `extensions/plugin_loader.py`：四钩子契约（`register(ctx)` 全控钩子优先，定义时只运行它；`register_tools`/`register_commands`/`register_skills` 专用钩子各拿对应注册表）、双通道发现（`mini_agent.plugins` entry-point 群组 + `plugin_dirs` 目录，默认 `./.mini-agent/plugins` 与 `~/.mini-agent/plugins`）、三层异常隔离（导入/钩子/运行时——坏插件绝不影响 Agent 主流程）、`disabled_plugins` 配置禁用开关、`/plugins` 命令展示各插件注册内容。插件工具有意不受 `enabled_tools` 内置白名单约束（安装插件即 opt-in）。`SkillRegistry.register()` 保证编程注册的技能在 `/skill reload` 后存活。示例插件见 `examples/plugins/word_count_plugin.py`。16 个新测试共 968 个；真实 LLM 实际调用插件工具验证通过。
- **Default agent type wired (P80)** — `DEFAULT_AGENT_TYPE` (extension point #10) now backs untyped sub-agent spawns: `SubAgent.__init__` falls back to the `worker` type's prompt/tool logic instead of a near-duplicate inline `SUBAGENT_SYSTEM_PROMPT` (deleted). Untyped spawns keep the user-tunable `config.max_agent_iterations` budget (not silently clamped to the worker profile's 50); explicit `--type` opt-in still adopts the full type profile. 4 new tests, 938 total; real-LLM verified via `experiments/verify_default_agent_type.py`. 默认 Agent 类型接线（扩展点 #10）：未指定类型的 SubAgent 派生回退 worker 类型档案的 prompt/工具逻辑，删除与之近乎重复的内联 `SUBAGENT_SYSTEM_PROMPT`；未指定类型时保留用户可配的 `config.max_agent_iterations` 迭代预算（不被 worker 档案的 50 静默钳制），显式 `--type` 仍采纳类型完整档案。4 个新测试共 938 个；经 `experiments/verify_default_agent_type.py` 真实 LLM 验证。
- **Tool-level permissions + universal check entry (P79)** — `PermissionScope.TOOL` (extension point #9) wired end to end: a tool-level gate runs before command/path checks for every tool call — a TOOL deny rule blocks the tool outright, a TOOL allow rule trusts it wholesale (skips resource checks, so even dangerous commands stop confirming), no match falls through to the existing routing. `/allow`/`/deny` accept the `tool` scope and `permissions.toml` gains a `[tools]` section (load + `--save`); `would_ask()` resolves tool rules without prompting. `PermissionManager.check()` (extension point #15) refactored into a true universal entry that dispatches by scope — COMMAND requests get dangerous-pattern confirmation, PATH requests go through PathGuard, instead of both silently bypassing them. `/allow remove` / `/deny remove` subcommands delete session rules by exact scope+pattern+level (TOML-sourced rules reload on next start); `[scope]` in command output is now escaped so markdown stops swallowing it as a reference link. 22 new tests, 934 total; real-LLM verified via `experiments/verify_tool_permission.py` (4 phases). 工具级权限与通用检查入口（扩展点 #9/#15 接入）：`PermissionScope.TOOL` 端到端接线——每次工具调用先过工具级门（在命令/路径检查之前）：TOOL deny 规则直接拦掉整个工具，TOOL allow 规则整体信任（跳过资源级检查，危险命令也不再弹确认），无匹配落回原有路由。`/allow` `/deny` 支持 `tool` scope，permissions.toml 新增 `[tools]` 节（加载与 `--save` 均支持）；`would_ask()` 对工具级规则直接判定不弹窗。`PermissionManager.check()` 重构为按 scope 分发的真正通用入口——COMMAND 请求走危险模式确认、PATH 请求走 PathGuard，不再双双静默绕过。`/allow remove` / `/deny remove` 子命令按 scope+pattern+level 精确删除会话规则（TOML 来源的规则下次启动仍加载）；命令输出中的 `[scope]` 已转义，markdown 不再吞成引用链接。22 个新测试共 934 个；经 `experiments/verify_tool_permission.py` 四阶段真实 LLM 验证。
- **Runtime permission rule management (P78)** — `PermissionManager.add_rule()` (extension point #3) enhanced from a one-liner stub to a full runtime API: validates empty patterns, deduplicates by scope+pattern+level, emits `PermissionRuleAddedEvent` via EventBus. New `remove_rule()` (with `PermissionRuleRemovedEvent`), `list_rules()`, and `save_rule_to_file()` (TOML persistence). `/allow` and `/deny` slash commands for interactive rule management (`/allow command "docker *"`, `--save` to persist to `.mini-agent/permissions.toml`). Internal callers (`_load_rules_from_config`, `load_rule_files`) unified through `add_rule(_silent=True)`. 13 new tests, 912 total. 运行时权限规则管理（扩展点 #3 接入）：`PermissionManager.add_rule()` 从一行桩增强为完整运行时 API——校验空模式、按 scope+pattern+level 去重、经 EventBus 发射 `PermissionRuleAddedEvent`；新增 `remove_rule()`（带 `PermissionRuleRemovedEvent`）、`list_rules()` 与 `save_rule_to_file()`（TOML 持久化）。`/allow` `/deny` 斜杠命令交互式管理规则（如 `/allow command "docker *"`，`--save` 持久化到 `.mini-agent/permissions.toml`）。内部调用方（`_load_rules_from_config`、`load_rule_files`）统一走 `add_rule(_silent=True)`。13 个新测试共 912 个。
- **Four mid-level extension points wired (P77)** — (1) `ToolRegistry.filter()` now used in `AgentTeam.start()` (non-writer steps) and `SubAgent.__init__` (tool whitelist), replacing ad-hoc list comprehensions; (2) `Plan.is_complete` drives the team loop (`while not plan.is_complete` replaces manual pending-list tracking); (3) `SessionMetadata.tags` wired with `/session tag`/`untag`/`tags` subcommands and `/session list --tag <name>` filtering (tags survive save/load, displayed in session list); (4) `PermissionRequest.tool_name` populated by `check_path(tool_name=tc.name)` so permission decisions carry the originating tool name for audit. 四个中级扩展点接入：(1) `ToolRegistry.filter()` 用于 `AgentTeam.start()` 非写文件步骤和 `SubAgent.__init__` 工具白名单，替代散落的列表推导；(2) `Plan.is_complete` 驱动团队循环（`while not plan.is_complete` 替代手动 pending 列表跟踪）；(3) `SessionMetadata.tags` 接入 `/session tag`/`untag`/`tags` 子命令与 `/session list --tag <name>` 过滤（标签随保存/加载存活并在会话列表显示）；(4) `PermissionRequest.tool_name` 由 `check_path(tool_name=tc.name)` 填充，权限判定携带发起工具名供审计。
- **Three extension points wired (P76)** — (1) `/model` now shows available providers via `ProviderRegistry.list_providers()`; (2) `UserMessageEvent.is_slash_command` set to `True` for slash commands, consumed by `AuditLogger` (audit trail) and `TraceRenderer` (trace display with `[slash]` tag); (3) `LLMRequestEvent.estimated_tokens` filled from `ContextManager.total_tokens`, displayed in trace as `~N tok`. 三个扩展点接入：(1) `/model` 经 `ProviderRegistry.list_providers()` 显示可用 Provider 列表；(2) 斜杠命令的 `UserMessageEvent.is_slash_command` 置 True，由 AuditLogger 记入审计轨迹、TraceRenderer 以 `[slash]` 标签显示；(3) `LLMRequestEvent.estimated_tokens` 从 `ContextManager.total_tokens` 填值，trace 显示 `~N tok`。

- **PermissionDecision.PENDING — cross-process permission protocol (P82)** — three-part feature: (1) Pane workers (`/spawn --pane`) now have a full permission stack (PathGuard + PermissionManager + RemoteConfirm) instead of auto-granting all tool calls — the worker writes `~/.mini-agent/workers/<id>.perm-request.json`, the parent process polls and relays through `terminal.confirm`, timeout 120s safely denies; (2) Remote/browser mode disconnect queuing — pending permission futures survive client disconnects, replay on reconnect, timeout deny after 120s; (3) `PermissionCheckEvent(decision="pending")` emitted before awaiting user response, `/trace` shows `PENDING (awaiting user)` in warning color. 15 new tests + E2E verified. 跨进程权限审批协议（扩展点 #8 接入），三部分：① pane worker（`/spawn --pane`）拥有完整权限栈（PathGuard + PermissionManager + RemoteConfirm），不再自动放行全部工具调用——worker 写 `~/.mini-agent/workers/<id>.perm-request.json`，父进程轮询并经 `terminal.confirm` 中转，超时 120s 安全拒绝；② 远程/浏览器模式断连排队——待决权限在客户端断连后存活，重连时重发，120s 超时拒绝；③ 等待用户响应前发射 `PermissionCheckEvent(decision="pending")`，`/trace` 以警告色显示 `PENDING (awaiting user)`。15 个新测试 + E2E 验证。

### Changed 变更

- **`Conversation.slice_window()` removed (P81)** — extension points #5/#16 resolved by deletion (they were duplicate rows for the same method): zero production callers, superseded by ContextManager/Compressor, and hazardous as-is (uncounted messages pass at zero cost so the budget never applies; tail slicing can orphan tool_use/tool_result pairs, which strict APIs reject with 400). Fixing it would duplicate `_compute_keep_split`. 删除 `Conversation.slice_window()`（扩展点 #5/#16 以删除方式收口——两者本是同一方法的重复条目）：零生产调用方、职责已被 ContextManager/Compressor 取代，且现状有害——未计数消息按零成本通过导致预算永不生效、尾部切片可能切断 tool_use/tool_result 配对（严格 API 以 400 拒绝）；修好它等于重抄 `_compute_keep_split`。
- **LLM streaming call extraction** — moved `assemble_response()` from `llm/openai_provider.py` to `llm/base.py` (it operates on base types, not provider-specific); added standalone `complete(llm, messages, ...)` function that wraps stream + assemble in one call; replaced 5 duplicated stream+assemble loops (extraction, recall, consolidation, compressor, planner) with `complete()`. `agent_loop._stream_once()` unchanged (has its own mid-stream callbacks). LLM 流式调用抽取：`assemble_response()` 从 `llm/openai_provider.py` 移至 `llm/base.py`（它只操作基础类型，与 provider 无关）；新增独立函数 `complete(llm, messages, ...)` 一次调用封装流式收集 + 组装；提取/召回/合并/压缩器/规划器五处重复的流式+组装循环统一替换为 `complete()`。`agent_loop._stream_once()` 保持不变（它有自己的流中回调）。
- **Forgotten code wiring (P75)** — six pieces of written-but-never-wired code connected: (1) `LLMResponse.model` set after `assemble_response`; (2) `CostTracker` cache-aware pricing via `cache_read`/`cache_creation` fields (avoids overcharging cached input); (3) `enable_plan_mode` config read at startup (default changed to `False`); (4) `on_thinking_delta` terminal callback (DeepSeek R1 reasoning now renders in dim italic); (5) `PermissionRequest.matched_rule` flows to `AuditLogger` via `PermissionCheckEvent`; (6) `ContextManager.count_message()` per-tool-call +3 overhead (dead `count_message_tokens`/`count_messages_tokens` deleted). 遗忘代码接入（6 处写而未接的代码）：(1) `LLMResponse.model` 在 `assemble_response` 后赋值；(2) `CostTracker` 经 `cache_read`/`cache_creation` 字段感知缓存差异化计费（避免缓存输入按全价多算）；(3) `enable_plan_mode` 配置启动时读取（默认值改为 `False`）；(4) `on_thinking_delta` 终端回调接线（DeepSeek R1 推理过程以暗斜体渲染）；(5) `PermissionRequest.matched_rule` 经 `PermissionCheckEvent` 流入 `AuditLogger`；(6) `ContextManager.count_message()` 按工具调用 +3 开销精确计数（死函数 `count_message_tokens`/`count_messages_tokens` 删除）。

### Fixed 修复

- **Compression pipeline defect chain (P69–P72)** — four defects exposed by real-terminal verification: Stage 1 no longer truncates tool results inside the keep window (the model perceived broken tools and spiraled into re-reads, 36 iterations → 4); the recovery-attachment budget scales with the context window (a 54K attachment pinned small windows); SlidingWindow anchors the freshly created summary so tail-based truncation never deletes it; the extractive digest strips baked-on recovery attachments before re-digesting (17K file dumps drowned planted conventions). 压缩链路缺陷链修复：真实终端验证暴露的四个缺陷——Stage 1 不再截断保留窗口内的工具结果（模型误以为工具坏了陷入重读螺旋，36 次迭代 → 4 次）；恢复附件预算随上下文窗口缩放（54K 附件曾钉死小窗口）；SlidingWindow 给刚生成的摘要加锚点，尾部截断不再删掉它；抽取式摘要重摘要前剥离已烙入的恢复附件（17K 文件转储曾淹没埋点约定）。
- **Spill cache permission popups** — reading back the agent's own spill files (`~/.mini-agent/cache/results/`) no longer prompts for confirmation (read-only auto-allow; writes still ask). The spill placeholder invites the LLM to read these files, so prompting on every read-back defeated the mechanism. 溢写缓存权限弹窗修复：读回 Agent 自己的溢写文件（`~/.mini-agent/cache/results/`）不再弹确认框（只读自动放行，写入仍询问）。溢写占位文案本就引导 LLM 去读这些文件，每次读回都弹窗等于废掉该机制。
- **Prompt label pollution after confirmation** — the permission dialog reused the main input's PromptSession; prompt_toolkit makes the passed message the session's new default, so after the first confirmation the main prompt permanently showed "allow? [y/a/n] >". Now uses a temporary session. 确认后提示符污染修复：权限对话框曾复用主输入的 PromptSession，而 prompt_toolkit 会把传入消息设为该会话的新默认提示——首次确认后主提示符永久变成 "allow? [y/a/n] >"。现改用临时 PromptSession。
- **Circuit breaker warning spam** — the compression circuit breaker warned on every check (twice per iteration) once open; now warns once on open and resets after an effective compression. 压缩熔断器警告刷屏修复：熔断器开启后每次检查都告警（每轮迭代两次）；现在只在开启时告警一次，有效压缩后自动复位。
- **bash GBK output mojibake** — subprocess output now decoded strict UTF-8 → active codepage/GBK → UTF-8 replace (three-tier), so Chinese CMD error messages render correctly. bash GBK 输出乱码修复：子进程输出按严格 UTF-8 → 活动代码页/GBK → UTF-8 replace 三级解码，中文 CMD 错误信息正确显示。
- **LLM autonomous git commands** — all git state-changing commands (commit/push/reset/stash/rebase/checkout/restore/clean) now require user confirmation (human-in-the-loop), plus CRITICAL system prompt rules. LLM 自主 git 命令闸门：全部 git 状态修改命令（commit/push/reset/stash/rebase/checkout/restore/clean）需用户确认（human-in-the-loop），并在 system prompt 增加 CRITICAL 红线规则。
- **Git Bash (mintty) instant exit** — piped stdin detected via isatty(), falls back to plain input mode (no completion menu; use `winpty mini` for the full experience). Git Bash（mintty）秒退修复：经 isatty() 检测管道 stdin，自动降级为朴素输入模式（无补全菜单）；`winpty mini` 可获完整体验。
- **Surrogates crash on GBK usernames** — lone surrogate chars (\udcXX) from GBK paths no longer crash the API request; messages are sanitized before JSON encoding. GBK 用户名代理字符崩溃修复：GBK 路径产生的孤立代理字符（U+DCxx）不再崩掉 API 请求——消息在 JSON 编码前统一净化。

### Added 新增

- **Event listener plugins (`EventBus.on_any`)** — drop a `.py` file into a `listener_dirs` directory (default `./.mini-agent/listeners` + `~/.mini-agent/listeners`) to observe all bus events with zero code changes: define `register(bus)` (full control) or `on_event(event)` (sync or async, auto-registered globally). Plugin import/registration/handler errors are isolated and logged, never breaking the agent; `EventBus.emit` now logs handler exceptions (previously swallowed) and `off_any()` added. 事件监听插件（`EventBus.on_any`）：把 `.py` 文件放进 `listener_dirs` 目录（默认 `./.mini-agent/listeners` + `~/.mini-agent/listeners`）即可零代码监听全部总线事件——定义 `register(bus)`（完全控制）或 `on_event(event)`（同步/异步均可，自动注册为全局监听）。插件导入/注册/handler 异常全部隔离并记日志，绝不影响 Agent；`EventBus.emit` 现在记录 handler 异常（此前被静默吞掉），并新增 `off_any()`。
- **Confirm hook rules (`HookAction.CONFIRM`)** — `[[hooks]]` config rules accept `action = "confirm"`: matching tool calls pop the y/a/n confirmation dialog instead of being rejected outright (y = allow once, a = never ask again this session for the same rule, n = deny with `Denied by user: <reason>` returned to the LLM). Resolution lives in the agent loop via an injected `terminal.confirm` callback (hooks hold no UI reference); no callback = safe deny; dialogs are lock-serialized against parallel tool execution, and streaming tool execution defers would-confirm calls until after the stream. Real-LLM verified (approve / deny / always paths). 确认型 hook 规则（`HookAction.CONFIRM`）：`[[hooks]]` 配置规则支持 `action = "confirm"`——命中的工具调用弹 y/a/n 确认框而非直接拒绝（y 放行一次，a 本会话同规则不再询问，n 拒绝并把 `Denied by user: <reason>` 回传 LLM）。裁决在 agent loop 经注入的 `terminal.confirm` 回调执行（hook 不持 UI 引用）；无回调时安全拒绝；弹窗对并行工具执行加锁串行；流式工具执行把需确认的调用延迟到流结束后。真实 LLM 三路径（放行/拒绝/always）验证。
- **Summary prompt-too-long shrink retry (P73)** — when the summarize request itself is rejected as too large (HTTP 400/413 or length keywords in the error), drop the oldest 20% of summarizable messages AND shrink the char cap 20%, then retry (max 3 shrink rounds, budget independent of transient retries); a prior compression summary at the head is never dropped; once shrinks are exhausted, fall back to the extractive digest immediately instead of re-sending the identical oversized request. Real-API verified end to end (genuine 400 → 2 shrinks → 9-section summary). 摘要 prompt 超长收缩重试（P73）：摘要请求本身因过大被拒（HTTP 400/413 或错误信息含长度关键词）时，丢弃最旧 20% 可摘要消息并把字符上限缩 20% 后重试（最多 3 轮收缩，与偶发重试预算相互独立）；头部的旧压缩摘要绝不丢弃（它是更早历史的唯一记录）；收缩穷尽后立即回退抽取式摘要，不再重发相同的超大请求。真实 API 端到端验证（真 400 → 2 轮收缩 → 9 节摘要产出）。
- **LLM structured summary + retry (P67/P72)** — the summarize prompt now demands an `<analysis>` scratchpad plus a 9-section `<summary>` block (only the summary is injected); transient failures retry twice before the extractive fallback; prior summaries are carried forward whole with recovery attachments stripped. LLM 结构化摘要 + 重试（P67/P72）：摘要 prompt 要求输出 `<analysis>` 思考草稿加 9 节结构化 `<summary>` 块，只有 summary 注入对话（草稿不进上下文）；偶发失败先重试 2 次再落抽取式回退；旧摘要整条前传并剥离恢复附件。
- **Token-driven keep window (P68)** — the compression keep-boundary is computed by accumulated tokens (floor 10K / min 5 messages / cap 40K, all scaling with the compression target) instead of a fixed 6 messages. token 驱动的保留窗口（P68）：压缩保留边界按累计 token 计算——下限 10K / 最少 5 条消息 / 硬顶 40K，且全部随压缩目标缩放——替代固定保留 6 条消息。
- **Aggregate tool result budget** — when a turn's cumulative tool-result chars exceed `aggregate_spill_chars` (default 200K, 0 = off), the largest results are force-spilled to disk largest-first until back under budget. Three supporting mechanisms: spill-file read-backs are exempt (anti-loop), preview enlarged 500→2000 chars, results no longer than the preview are never spilled. Spill placeholder now includes the spill file path for offset/limit re-reads. 聚合工具结果预算：单轮累计工具结果字符超过 `aggregate_spill_chars`（默认 200K，0 = 关闭）时，按最大优先强制溢写磁盘直到回到预算内。三个配套机制：溢写文件读回豁免（防死循环）、预览从 500 扩到 2000 字符、不长于预览的结果永不溢写。溢写占位文案带溢写文件路径，供 offset/limit 精读。
- **Memory export/import** — `/memory export [dir]` writes each memory as a standalone .md file (YAML frontmatter: id/source/scope/created_at/tags) plus a MEMORY.md index; `/memory import <dir>` imports with id dedup and restores project/user scope from frontmatter. Tolerant parsing accepts plain .md and mewcode-style files. 记忆导出/导入：`/memory export [dir]` 把每条记忆写成独立 .md 文件（YAML 前置元数据：id/source/scope/created_at/tags）外加 MEMORY.md 索引；`/memory import <dir>` 按 id 去重导入并从前置元数据还原 project/user 作用域。容错解析接受纯 .md 与 mewcode 风格文件。
- **Compression tool-pair alignment** — the summarize keep-boundary backs up to the tool-pair head so tool_use/tool_result pairs are never split (strict APIs reject orphans with 400); SlidingWindow drops leading orphan tool results. 压缩工具对对齐：摘要保留边界回退到工具对头部，tool_use/tool_result 配对永不切断（严格 API 对孤儿以 400 拒绝）；SlidingWindow 丢弃开头的孤儿 tool result。
- **Compression circuit breaker** — after N consecutive ineffective compressions (tokens did not decrease), `check_and_compress()` skips further attempts. Prevents wasted compression cycles when `_inject_read_files` cancels out compression gains (e.g., 150+ read files). `ensure_fits()` hard fallback unaffected. Config: `compress_max_failures = 3` (0 = off). 压缩熔断器：连续 N 次无效压缩（token 未减少）后 `check_and_compress()` 跳过后续尝试，防止 `_inject_read_files` 抵消压缩收益（如 150+ 已读文件）时的空转循环；`ensure_fits()` 硬兜底不受影响。配置 `compress_max_failures = 3`（0 = 关闭）。
- **Compression-reread inflation root fix** — two-layer defense: tool results >50K chars spill to disk (conversation keeps a 2000-char preview incl. the spill file path); after compression the summary carries a "files already read" list so the LLM does not re-read them. Configurable via `[memory] spill_threshold_chars` (0 = off). 压缩-重读膨胀根治：双层防御——超 50K 字符的工具结果溢写磁盘（对话只留 2000 字符预览，含溢写文件路径）；压缩后摘要携带"已读文件清单"，LLM 不再重读这些文件。配置 `[memory] spill_threshold_chars`（0 = 关闭）。
- **`/memory delete`** — delete memories by ID or content keyword; ambiguous matches list candidates instead of deleting. `/memory delete`：按 ID 或内容关键词删除记忆；多重匹配时列出全部候选而不是直接删除。
- **Same-tool per-iteration fuse** — second circuit breaker layer: a tool name appearing in every one of the last 15 iterations (args ignored) stops the loop; parallel batch reads within few iterations are unaffected. same-tool 按轮熔断：第二层熔断——同一工具名在最近 15 轮迭代中每轮都出现（忽略参数差异）即停止循环；少数迭代内的并行批量读取不受误伤。

- **Anthropic prompt caching** — three `cache_control: ephemeral` markers (system prompt, last tool, last user message) enable API-side caching; subsequent requests pay ~10% for cached input tokens. Cache hit/creation stats parsed into `TokenUsage`. Anthropic prompt 缓存：三处 `cache_control: ephemeral` 标记（system prompt、最后一个工具、最后一条用户消息）启用 API 侧缓存，后续请求缓存命中的输入 token 约按 10% 计费；缓存命中/创建统计解析进 `TokenUsage`。
- **Streaming tool execution** — tool calls execute the moment they finish assembling mid-stream (tool #1 runs while tool #2 still streams); tools needing confirmation are deferred until after the stream. Toggle: `streaming_tool_execution` (default on). 流式工具执行：工具调用在流式响应中一组装完成即执行（工具 1 运行时工具 2 还在流式传输）；需要确认的工具延迟到流结束后执行。开关 `streaming_tool_execution`（默认开启）。
- **@file inline references** — type `@README.md` in your message to auto-inline the file content (10KB cap, Tab completion with directory drilling). Saves a round-trip LLM read_file call. @文件内联引用：消息中输入 `@README.md` 自动内联文件内容（10KB 上限，Tab 补全支持目录逐级下钻），省掉一轮 read_file 工具调用往返。
- **Permission rule files** — user-defined allow/deny rules in `~/.mini-agent/permissions.toml` (user) and `.mini-agent/permissions.toml` (project); DENY > ALLOW > built-in defaults. Also fixes PATH deny rules being bypassed by the project-dir allow shortcut. 权限规则文件：用户级 `~/.mini-agent/permissions.toml` 与项目级 `.mini-agent/permissions.toml` 自定义 allow/deny 规则，优先级 DENY > ALLOW > 内置默认；同时修复项目内路径 allow 捷径绕过 PATH deny 规则的盲区。
- **OS-level sandbox** — Linux bubblewrap (bwrap) + macOS Seatbelt (sandbox-exec) kernel isolation for bash commands: read-only rootfs with writable working dir + /tmp. `sandbox_auto_allow` skips confirmation for dangerous commands (kernel provides isolation), but explicit deny rules still block. Config: `[security] sandbox = true`. OS 级沙箱：Linux bubblewrap（bwrap）+ macOS Seatbelt（sandbox-exec）对 bash 命令内核级隔离——只读根文件系统 + 可写工作目录与 /tmp；`sandbox_auto_allow` 让危险命令免确认（隔离由内核保证），显式 deny 规则仍拦截。配置 `[security] sandbox = true`。
- **Context window API probing** — OpenAI provider probes GET `{base_url}/models/{model}` at startup and after `/model` switch (new `LLMProvider.prepare()` hook, no-op by default) to discover the real context window; recursively extracts 5 field-name variants (context_window/context_length/max_context_length/max_model_len/max_input_tokens) at any nesting depth. Silent fallback: probed → built-in table → 128k. New models work without code changes. 上下文窗口 API 探测：OpenAI provider 在启动时和 `/model` 切换后 GET `{base_url}/models/{model}`（经新增的 `LLMProvider.prepare()` 钩子，默认空操作）获取真实上下文窗口；在任意嵌套深度递归提取 5 种字段名变体（context_window/context_length/max_context_length/max_model_len/max_input_tokens）。静默回退链：探测值 → 内置表 → 128k。新模型零代码适配。
- **Token counting accuracy** — API usage anchoring: the LLM-reported usage total (which covers tool schemas estimation cannot see) anchors the conversation count; only messages after the anchor are estimated, so error no longer accumulates. CJK-aware estimation replaces bare len//4 (real-API test: Chinese was underestimated 56% — the dangerous direction, compression never fired). Also fixes assistant token_count storing total_tokens (N-fold double counting) and Anthropic's split usage events being overwritten during assembly. Token 计数精度：API usage 锚定——以 LLM 上报的用量总数（涵盖估算看不到的工具 schema）为对话计数锚点，只估算锚点之后的消息，误差不再累积；CJK 感知估算替代裸 len//4（真实 API 实测中文被低估 56%——低估是危险方向，压缩永远不触发）。另修复 assistant 的 token_count 误存 total_tokens（N 倍重复计数）与 Anthropic 分拆 usage 事件在组装时被覆盖两个 bug。
- **max_tokens recovery** — when a response is cut off (finish_reason "length"), the request retries with a doubled max_tokens (up to 3 times, 4096 → 32768), keeping the last result if still truncated. Anthropic's stop_reason="max_tokens" is normalized to "length" so recovery works for both providers. Mid-stream tool tasks from the truncated attempt are cancelled (their JSON args may be cut off). max_tokens 恢复：响应被截断（finish_reason="length"）时把 max_tokens 翻倍重试（最多 3 次，4096 → 32768），仍截断则保留最后一次结果；Anthropic 的 stop_reason="max_tokens" 归一化为 "length" 使两家 provider 都能恢复；被截断尝试的流中工具任务一并取消（其 JSON 参数可能被切断）。
- **Coordinator mode** — `/team --coordinator` restricts the Planner to pure task decomposition: coordinator prompt prefix ("you ONLY decompose and assign"), max_steps relaxed to 8 (cannot self-patch, needs finer granularity), and project scan deepened to 3 levels/120 lines (richer context since the coordinator cannot read files itself). Workers retain full tool access. Coordinator 模式：`/team --coordinator` 把 Planner 限制为纯任务分解——coordinator prompt 前缀（只分解与分派）、max_steps 放宽到 8（不能自己补丁，需要更细粒度）、项目扫描加深到 3 层/120 行（coordinator 自己不能读文件，需要更丰富的上下文）；Workers 保留全部工具权限。
- **Pydantic Schema generation** — 7 core tools now auto-generate JSON schemas from Pydantic BaseModels (ReadFile, WriteFile, EditFile, DeleteFile, Glob, Grep, SpawnAgents). Reduces boilerplate, adds type safety (automatic string→int coercion), and ensures schema stays in sync with parameter validation. BashTool kept hand-written for backward compatibility; Tool.schema can still be overridden. Pydantic Schema 生成：7 个核心工具（ReadFile/WriteFile/EditFile/DeleteFile/Glob/Grep/SpawnAgents）从 Pydantic BaseModel 自动生成 JSON schema——减少样板、类型安全（字符串→数字自动转换）、schema 与参数校验永远同步。BashTool 保持手写以向后兼容；Tool.schema 仍可覆盖。
- **Pydantic Schema full enhancement** — `_schema_from_model()` rewritten as raw JSON Schema passthrough: `$ref/$defs` resolution, `title` stripping, full support for Optional/anyOf, array items, nested models, Field constraints, Literal, dict, default values, circular ref protection. Pydantic Schema 全面增强：`_schema_from_model()` 重写为 raw JSON Schema 直通——`$ref/$defs` 解析、`title` 剥离，完整支持 Optional/anyOf、数组 items、嵌套模型、Field 约束、Literal、dict、默认值与循环引用保护。
- **Agent Type Definition** — `AgentTypeDefinition` frozen dataclass with 4 built-in types (explore/plan/worker/verify); SubAgent/SubAgentManager/SpawnAgentsTool accept `agent_type` parameter; `/spawn --type <name>` flag. Each type defines: system prompt, tool whitelist, iteration cap. Agent 类型定义：`AgentTypeDefinition` 冻结 dataclass + 4 种内置类型（explore/plan/worker/verify），每种类型定义 system prompt、工具白名单和迭代上限；SubAgent/SubAgentManager/SpawnAgentsTool 均接受 `agent_type` 参数；新增 `/spawn --type <name>` 旗标。
- **Plan mode read-only** — `/plan [on|off]` toggles physical read-only enforcement: write tool schemas hidden from LLM + execution blocked as double safety. System prompt injection on toggle. Plan 模式只读：`/plan [on|off]` 切换物理只读强制——写类工具的 schema 对 LLM 隐藏 + 执行层拦截双保险；切换时注入 system prompt 说明。
- **Hook lifecycle expansion** — HookStage grows to 11 stages, all actually fired: new STARTUP/SHUTDOWN/TURN_START/TURN_END + wired up previously-dead POST_LLM/SESSION_START/USER_INPUT. USER_INPUT supports BLOCK to intercept a turn. Hook 生命周期扩充：HookStage 增至 11 个阶段且全部实际触发——新增 STARTUP/SHUTDOWN/TURN_START/TURN_END 四个生命周期阶段，接线此前已定义但从未触发的 POST_LLM/SESSION_START/USER_INPUT；USER_INPUT 支持 BLOCK 拦截整轮输入。
- **Tool search / lazy loading** — MCP servers can use `loading = "dispatch"` to keep tools out of the LLM context; the LLM discovers them via `tool_search` and invokes them via `mcp_call`. Two new builtin tools (10 total). 工具搜索/延迟加载：MCP 服务器可配 `loading = "dispatch"`，其工具不进 LLM 上下文；LLM 经 `tool_search` 按需发现、经 `mcp_call` 按需调用。新增两个内置工具（当时共 10 个）。
- **Selective memory recall** — when stored memories exceed `recall_threshold` (10), a lightweight LLM call picks the `recall_top_k` (5) most relevant entries to inject instead of head-truncating. Fail-safe fallback to the old behavior on any error. 选择性记忆召回：存量记忆超过 `recall_threshold`（10）时，轻量 LLM 调用挑选 `recall_top_k`（5）条最相关条目注入，替代头部截断；任何错误静默回退旧行为。
- **Memory consolidation** — when memories exceed `consolidation_threshold` (20), an LLM merges semantically related entries into one (keeps newest timestamp, unions tags). Also available manually via `/memory consolidate`. Fail-safe no-op on any error. 记忆合并：记忆超过 `consolidation_threshold`（20）时 LLM 把语义相关条目合并为一条（保留最新时间戳、tags 取并集）；也可 `/memory consolidate` 手动触发；任何错误安全空操作。
- **Worktree lifecycle** — new worktrees auto-symlink `node_modules`/`.venv`/`vendor` (skip reinstalls); stale clean worktrees older than `worktree_max_age_days` (7) are removed at startup (dirty ones kept); `/spawn wait` shows the worktree path with a `git merge` hint. Worktree 生命周期：新 worktree 自动符号链接 `node_modules`/`.venv`/`vendor`（跳过依赖重装）；超过 `worktree_max_age_days`（7 天）的过期干净 worktree 启动时清理（有未提交改动的保留）；`/spawn wait` 显示 worktree 路径并附 `git merge` 提示。
- **Skill install/uninstall** — `/skill install <path_or_url>` installs a skill from a local directory or git URL into `~/.mini-agent/skills/` with SKILL.md validation (auto-cleanup on failure); `/skill uninstall <name>` removes by name. Skill 安装/卸载：`/skill install <path_or_url>` 从本地目录或 git URL 安装技能到 `~/.mini-agent/skills/`，SKILL.md 格式校验（失败自动清理残留）；`/skill uninstall <name>` 按名卸载。
- **Skill hot reload** — `/skill reload` rescans disk, updates active skill prompts in-place (strips old, injects new), and reports lost skills whose files were deleted. `load_all()` now clears before rescan so stale entries don't accumulate. Skill 热重载：`/skill reload` 重扫描磁盘、就地更新活跃技能的 prompt（剥旧注新），并报告文件已被删除的丢失技能；`load_all()` 重扫前先清空，过期条目不再累积。
- **Remote/Browser mode** — `--remote` starts a WebSocket + HTTP server; open `http://localhost:8765` in a browser to use the agent remotely. 12-type NDJSON protocol, embedded dark-theme UI with Markdown rendering (h1-h4, lists, tables, links, bare URLs, images), thinking indicator, permission dialogs (Allow/Always/Deny with click feedback), history replay on refresh, HTTP-based cancel and permission (bypasses WS blocking), optional token auth (`--remote-token`), multi-client broadcast. Optional dependency: `websockets` (`uv sync --extra remote`). 远程/浏览器模式：`--remote` 启动 WebSocket + HTTP 服务器，浏览器打开 `http://localhost:8765` 即可远程使用 Agent。12 种事件的 NDJSON 协议；内嵌深色主题 UI 支持 Markdown 渲染（h1-h4/列表/表格/链接/裸 URL/图片）、thinking 指示器、权限对话框（Allow/Always/Deny 带点击反馈）、刷新时重放历史、HTTP 取消与权限端点（绕开 WS 阻塞）、可选 token 认证（`--remote-token`）、多客户端广播。可选依赖 `websockets`（`uv sync --extra remote`）。

### Fixed 修复（P36 实战补修）

- **Task anchor in truncation** — SlidingWindow now always keeps the latest user message; a long turn (one question + dozens of tool results) could push the question out of the window, leaving the LLM asking "what did you want?". 截断保任务锚点：SlidingWindow 永远保留最近一条用户消息——长轮次（一个问题 + 几十条工具结果）曾把问题本身挤出窗口，LLM 反问"你要我做什么"。
- **Respond in user's language** — system prompt now instructs the LLM to always answer in the language the user writes in. 用用户的语言回答：system prompt 明确要求 LLM 始终使用用户书写所用的语言作答。

### Experiments 实验

- **Deadlock induction** — 5 scenarios × 2 arms testing triple fuse under real LLM. Key finding: iteration limit is the only reliable hard fuse; same-tool-6x never triggered (LLM varies arguments each time). 死循环诱导实验：5 场景 × 2 臂真实 LLM 实测三重熔断。关键发现：迭代上限是唯一可靠的硬熔断；same-tool-6x 从未触发（LLM 每次微调参数绕过签名检测）。
- **Circuit breaker verification** — 5-phase real-LLM test: normal compression / 150-file read_files triggers natural breaker / ensure_fits fallback / disabled control group / new-session recovery. 压缩熔断器验证：5 阶段真实 LLM 实验——正常压缩 / 150 个已读文件触发自然熔断 / ensure_fits 兜底 / 关闭对照组 / 新会话恢复。

### Docs 文档

- New `docs/terminal-guide.md` — how to open each terminal per OS (Windows/macOS/Linux), compatibility levels, troubleshooting table. 新增 `docs/terminal-guide.md`：各系统（Windows/macOS/Linux）每种终端的打开方法、兼容等级与问题排查表。

## v1.0.0

### Interface Freeze 接口冻结

The following ABCs and type aliases are now **stable**. Their method
signatures will not change without a major version bump.
以下 ABC 与类型别名现已**稳定**：不经历主版本号升级，其方法签名不会改变。

#### Tool (`tools/base.py`)

```python
class Tool(ABC):
    @property
    @abstractmethod
    def schema(self) -> ToolSchema: ...

    @abstractmethod
    async def execute(self, ctx: ToolContext, **kwargs: Any) -> ToolResult: ...
```

#### LLMProvider (`llm/base.py`)

```python
class LLMProvider(ABC):
    @abstractmethod
    async def stream(
        self, messages: list[dict[str, Any]],
        tools: list[dict[str, Any]] | None = None, **kwargs: Any
    ) -> AsyncIterator[StreamChunk]: ...

    @abstractmethod
    def count_tokens(self, text: str) -> int: ...

    @property
    @abstractmethod
    def context_window(self) -> int: ...
```

#### HookFn (`tools/hooks.py`)

```python
HookFn = Callable[[HookContext], Awaitable[HookResult]]
```

#### CompressionStrategy (`memory/compressor.py`)

```python
class CompressionStrategy(ABC):
    @abstractmethod
    async def compress(
        self, conversation: Conversation, target_tokens: int
    ) -> None: ...
```

### Supporting Types 支撑类型

These dataclasses are part of the stable interface: 以下 dataclass 属于稳定接口的一部分：

- `ToolSchema`, `ToolParameter`, `ToolContext`, `ToolResult`
- `StreamChunk`, `TokenUsage`, `LLMResponse`, `ToolCallDelta`
- `HookContext`, `HookResult`, `HookAction`, `HookStage`
- `Conversation`, `Message`, `ToolCall`

### What "frozen" means 冻结的含义

- Method signatures (names, parameter types, return types) will not change 方法签名（名称、参数类型、返回类型）不会改变
- New **optional** parameters may be added with defaults 可以新增带默认值的**可选**参数
- New methods may be added to ABCs (existing ones won't change) ABC 可以新增方法（既有方法不变）
- Breaking changes require a major version bump (2.0.0) 破坏性变更必须升主版本号（2.0.0）

### Features 功能

- P1-P34: 34 development phases completed (see README.md for full list) 34 个开发阶段完成（完整清单见 README.md）
- 425 tests, zero external dependencies for testing 425 个测试，测试零外部依赖
- Per-turn file change summary (+created / ~modified / -deleted) 轮末文件变更汇总（+新建 / ~修改 / -删除）
- Colored diff preview for edit_file (full-width background highlight) edit_file 彩色 diff 预览（整行背景高亮）
- 8 built-in tools (read/write/edit/delete/bash/glob/grep/spawn_agents) 8 个内置工具
- Multi-agent orchestration (/spawn, /team, spawn_agents tool) 多 Agent 编排（/spawn、/team、spawn_agents 工具）
- Mechanism experiments (compression A/B, strong/weak model mixing) 机制实验（压缩 A/B、强弱模型混编）
- Session auto-save with crash recovery 会话自动保存与崩溃恢复
- Theme system (default/dark/light) 主题系统（default/dark/light）
- TOML configuration (user-level + project-level) TOML 配置（用户级 + 项目级）
- Audit logging with hash-chain tamper detection 审计日志（哈希链防篡改）
- Cost dashboard: per-model pricing, session + all-time ledger, dual budgets with 80%/100% warnings (/cost, /cost turns, /cost reset) 成本仪表盘：分模型计价、会话 + 累计总账、双预算 80%/100% 警告
